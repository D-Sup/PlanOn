// import { selector } from "recoil";
// import { postFormValue } from "../atoms/postFormValueAtom";

// export const isPostFormModifiedSelector = selector({
//   key: "isPostFormModifiedSelector",
//   get: ({get}) => {
//     const form = get(postFormValue);
//     const isModified = 
//     form.photos.checked.length > 0 || 
//     form.photos.file.length > 0 || 
//     form.photos.preview.length > 0 || 
//     form.scheduleId !== "" || 
//     form.private !== false || 
//     form.hashtags.length > 0 || 
//     form.usertags.length > 0 || 
//     form.content !== "";
    
//     return isModified;
//   },
// });
